AI Color & Mood Analyzer

Extracts dominant colors from any uploaded photo using K-Means clustering and predicts the image's mood based on brightness, saturation, and warmth.

Run locally

pip install -r requirements.txt
streamlit run app.py
